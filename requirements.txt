# MCP server (Claude Code integration) — this is the primary use case
mcp>=1.0
yt-dlp>=2024.1.1

# Optional: keep Flask + gunicorn only if you also want the web UI on Linode
# flask>=3.0
# gunicorn>=22.0
